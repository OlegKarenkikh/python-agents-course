# ============================================
# Практика: Первый запрос к LLM
# ============================================
# Задача: напишите программу, которая:
# 1. Получает текст заявки от пользователя (input)
# 2. Отправляет его в LLM с системным промптом «Инспектор заявок»
# 3. Выводит ответ модели
#
# Для запуска установите:
#   pip install openai
#
# Задайте токен:
#   В GitHub Codespaces: GITHUB_TOKEN доступен автоматически
#   Локально: получите токен на https://github.com/settings/tokens

import os
from openai import OpenAI

SYSTEM_PROMPT = """Ты — инспектор заявок. Проверяй текст заявки
и отвечай: полная ли она? Если нет, что отсутствует?"""


def check_with_llm(text: str) -> str:
    """TODO: реализуйте запрос к LLM."""
    github_token = os.environ.get("GITHUB_TOKEN", "")
    if not github_token:
        return "Ошибка: GITHUB_TOKEN не установлен."
    
    # TODO: создайте client, отправьте запрос, верните ответ
    pass


if __name__ == "__main__":
    текст = input("Введите текст заявки: ")
    результат = check_with_llm(текст)
    print("Ответ модели:", результат)
